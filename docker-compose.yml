version: '3'
services:
  web:
    build: .
    ports:
      - "${SERVERPORT}:${SERVERPORT}"
  postgres:
    image: 'postgres:12.3'
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - ${PGPORT}
